# Router.run(routes, Router.HashLocation, (Root) =>
#   React.render(React.createElement(Root), document.getElementById('search'))
# )
# React.render React.createElement(Search, null), document.getElementById('search')
